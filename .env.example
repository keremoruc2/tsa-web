# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/taut_db?schema=public"

# SMTP Settings for Email (Optional - emails will be logged to console if not configured)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="TAUT <noreply@taut.nl>"

# Admin email to receive membership notifications
ADMIN_EMAIL="info@taut.nl"

# Optional: Analytics (if you want to add later)
# NEXT_PUBLIC_GA_ID=""
